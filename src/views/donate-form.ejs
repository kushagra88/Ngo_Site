<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Donation Form</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      background-image: "images/hijiji.jpg";
    }

    .container {
      max-width: 600px;
      margin: 20px auto;
      padding: 20px;
      background-color: #fff;
      border-radius: 5px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }

    input[type="text"],
    input[type="email"],
    input[type="tel"],
    input[type="number"],
    select {
      width: 100%;
      padding: 10px;
      margin: 8px 0;
      border: 1px solid #ccc;
      border-radius: 4px;
      box-sizing: border-box;
    }

    button {
      background-color: #21e427;
      color: white;
      padding: 10px 20px;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      display: block;
      margin: auto;

    }

    button:hover {
      background-color: #5d835f;
    }
  </style>
</head>

<body>
  <div class="container">
    <h2>Donation Form</h2>
    <form id="donationForm" method="post" action="/submit-donation">
      <label for="firstName">First Name:</label>
      <input type="text" id="name" name="firstName" required>
      <label for="lastName">Last Name:</label>
      <input type="text" id="name" name="lastName" required>

      <label for="email">Email:</label>
      <input type="email" id="email" name="email">

      <label for="phone">Phone:</label>
      <input type="number" id="phone" name="phone" required>

      <label for="address">Address:</label>
      <input type="text" id="address" name="address" required>

      <label for="items">Select the donation:</label>
      <select name="donation" id="donation">
        <option value="Select">Select the Donation</option>
        <option value="books">Books</option>
        <option value="clothes">Clothes</option>
        <option value="medicine">Medicine</option>
        <option value="money">Money</option>
        <option value="shoes">Shoes</option>
      </select>
      
      <label style="display: none;" for="amount" id="amountLabel" class="amount">Amount:</label>
      <input style="display: none;" type="number" id="amountInput" name="amount" placeholder="Minimum amount = 250 RS" disabled>

      <button type="submit" id="submitButton">Proceed To Payment</button>
    </form>
    <script>
      //for enable or disable of amount lable and input field
      function handleDonationChange() {
        const donationSelect = document.getElementById('donation');
        const amountLabel = document.getElementById('amountLabel');
        const amountInput = document.getElementById('amountInput');

        if (donationSelect.value == 'money') {
          amountLabel.style.display = 'block';
          amountInput.style.display = 'block';
          amountInput.disabled = false;
        } else {
          amountLabel.style.display = 'none';
          amountInput.style.display = 'none';
          amountInput.disabled = true;
        }
      }
      //amount validation
      // const form = document.getElementById('donationForm'); 
      // form.addEventListener('submit', function (event) {
      //   event.preventDefault();

      //   const donationSelect = document.getElementById('donation');
      //   const amount = parseFloat(amountInput.value);

      //   if (donationSelect.value === 'money' && amount < 250) {
      //     alert('Please enter an amount of 250 or more.');
      //     return;
      //   }

      //   // Proceed with form submission or other actions
      //     alert('Form submitted successfully!');
      // });

      // //for console.log
      // form.addEventListener('submit', function (event) {
      //   event.preventDefault();

      //   const firstName = form.elements.firstName.value;
      //   const lastName = form.elements.lastName.value;
      //   const email = form.elements.email.value;
      //   const phone = form.elements.phone.value;
      //   const address = form.elements.address.value;
      //   const donation = form.elements.donation.value;


      //   if (donation == 'money') {
      //     const amount = form.elements.amount.value;
      //     if(amount>=250){
      //     console.log(`User input: First Name: ${firstName}, Last Name: ${lastName}, Email: ${email}, Phone: ${phone}, Address: ${address}, Donation: ${donation}, Amount: ${amount}`);
      //     }
      //   }
      //   else {
      //     const amount = 0;
      //     console.log(`User input: First Name: ${firstName}, Last Name: ${lastName}, Email: ${email}, Phone: ${phone}, Address: ${address}, Donation: ${donation}, Amount: ${amount}`);
      //   }
      // })
    </script>
</body>

</html>